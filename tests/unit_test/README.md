# Unit Tests

Run the project unit tests from the repository root:

```powershell
node --test --test-isolation=none "tests/unit_test/*.test.mjs"
```

The suite uses Node's built-in test runner and does not require extra test dependencies.

